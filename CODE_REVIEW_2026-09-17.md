# ผลรีวิวโค้ด 2026-09-17

รีวิวงานของวันที่ 17 ก.ย. ทั้งสอง repo (`pos-api-new` `b4c8991..fe8015a`,
`pos-fe-new` `d2516c4..b9e7292`) ด้วยผู้รีวิว 4 มุม: ไล่ทีละบรรทัด, ตรวจของที่ถูกลบ,
ไล่ผู้เรียกข้ามไฟล์, ความปลอดภัย และประสิทธิภาพ

ทุกข้อด้านล่างยืนยันด้วยการอ่านโค้ดจริงและ/หรือ query ข้อมูล UAT แบบอ่านอย่างเดียว

---

## แก้แล้ว

| # | เรื่อง | commit |
|---|---|---|
| 1 | `card_play_service.go:227` ใส่ `card_play.id` ลงช่อง `card_deposit id` → บัตร Package/Time-play เล่นฟรี | `e0917dc` |
| 2 | `releaseClaim` ถอนการจอง `pos_void` ทั้งที่คืนยอดไปแล้วบางส่วน → กดซ้ำแล้วหักซ้ำ | `20ff6ae` |
| 3 | `refund_confirm` ยืนยันซ้ำได้ + goroutine ขนานแบบไม่มีใครหยุดใคร | `20ff6ae` |
| 4 | Path traversal ที่ BFF (ข้อ 1 ด้านล่าง) | `e46fa79` 18 ก.ย. |
| 5 | route ปรับแต้มสมาชิกไม่มี `RequireRole` (ข้อ 2 บางส่วน) | `7647056` 18 ก.ย. |
| 6 | หน้า `/adjust` ไม่มีด่านหัวหน้า + ชื่อผู้ทำรายการปลอมได้ | `5e95f5e` `844fd4e` 18 ก.ย. |

ข้อ 1 เป็น **regression ที่เกิดจาก `aff52a5` ของวันเดียวกัน** — guard `RowsAffected == 0`
ทำให้แถวที่ใส่ id ผิดตาราง (ซึ่งเดิมไม่มีพิษภัยเพราะ UPDATE ไม่โดนแถวไหนแล้วเงียบผ่านไป)
กลายเป็นตัวแรกของลูปที่คืน error เสมอ ของจริงจึงไม่เคยถูกหัก และผู้เรียกที่
`v2/card_entity_controller.go:220` รันแบบ fire-and-forget หลังตอบ success ไปแล้ว

หลักฐาน UAT: `card_deposit` ที่ id ตรงกับ `card_play.id` = **0 จาก 2678**,
`card_withdraw` ที่ `card_deposit_id` ไม่มีอยู่จริง = **221 แถว**,
`card_play_type` ที่ยังใช้งาน = **533 แถว**, foreign key บน `card_withdraw` = **ไม่มี**

---

## สถานะรายข้อ (อัปเดต 24 ก.ย.)

ข้อ 1, 3, 4, 5, 6, 8, 9, 10 แก้แล้ว · ข้อ 2 แก้บางส่วนและถือเป็นข้อยุติ ·
ข้อ 7 เป็นการวัดผล ไม่ใช่การแก้

**ครบทุกข้อในรายงานแล้ว** งานที่เหลือเป็นของใหม่ที่เจอระหว่างทาง —
ดูหัวข้อ 🔴 ท้ายข้อ 9 (แถว `users.id = 24`)

หมายเหตุ 24 ก.ย.: บรรทัดเดิมเขียนว่า "ข้อ 3 ไม่เกี่ยว (ตรวจแล้วไม่ใช่ข้อบกพร่อง)"
ซึ่งขัดกับเนื้อข้อ 3 เอง — ที่ไม่เกี่ยวคือ**ตอนนั้นบล็อกยังหลับอยู่** ไม่ใช่ตัวบั๊กไม่มีจริง
พอ `fbcd244` แก้เงื่อนไขที่กลับด้าน บล็อกก็ทำงานทุกครั้ง และบั๊กก็เกิดจริงในวันเดียวกัน
(บัตร `F443416C` ดูข้อ 3) แก้แล้ว `aa99141`

### ~~1. Path traversal ที่ BFF → เลื่อนขั้นตัวเองเป็น Administrator ได้~~ — แก้แล้ว `e46fa79`

`pos-fe-new/src/app/api/branch-group/[id]/route.js:39` และอีก **40 จุด** ยัดพารามิเตอร์
ลง URL ปลายทางโดยไม่ `encodeURIComponent`

ทดสอบจริงแล้ว: `new URL("http://backend:8080/api/branch-group/../user/42")`
ยุบเหลือ `/api/user/42`

แคชเชียร์ที่โดน 403 ที่ `/api/adjust-point` ส่ง
`PUT /api/branch-group/..%2Fuser%2F42` พร้อม body `{"user_role_id": 1}`
Next ถอดรหัส segment เป็น `../user/42` แล้ว `fetch` ยุบ path ไปโผล่ที่ `UpdateUser`
ซึ่งผ่าน `RequireRole(RoleAdministrator)` ได้ เพราะ BFF ยิงด้วย service account
`pos_frontend` ที่เป็น role 1 — **ลบล้าง RequireRole ทั้งชุดที่เพิ่งทำ**

route ที่รับค่าจาก query string (เช่น `card/check/package`) ง่ายกว่าอีก
เพราะส่ง `../` ตรง ๆ ได้โดยไม่ต้อง encode

**แก้แล้ว 18 ก.ย.** สองชั้น: ห่อ `encodeURIComponent` 22 จุดใน 19 ไฟล์ (เฉพาะส่วน path
ก่อน `?` — query ใช้ `buildQuery`/`URLSearchParams` ปลอดภัยอยู่แล้ว) และเพิ่ม
`upstreamPathEscape()` ใน `fetchUpstream` เป็นด่านรับพลาด ปฏิเสธก่อนยิงถ้า path ดิบ
มี segment `..` / `.` หรือมี `%2e` (ไม่ปฏิเสธ `%2f` เพราะ `encodeURIComponent`
สร้างมันขึ้นมาตามปกติ) ทดสอบ 9 เคสผ่านหมด

### 2. route ที่ขยับยอดเงินแต่ไม่ได้ติด `RequireRole` — แก้บางส่วน `7647056`

| route | ผล | สถานะ |
|---|---|---|
| `POST /api/member/:tel` (`UpdateSkill`) | `UPDATE member SET mskill1 = mskill1 + ?` ไม่มีเพดาน ไม่ตรวจเครื่องหมาย สร้างแต้มได้ไม่จำกัด ไม่มีแถว `adjust_point` จึงไม่โผล่ในรายงานที่หัวหน้าตรวจ | **แก้แล้ว** |
| `POST /api/member/deduct-joylicoin/:tel` | ล้างยอด Joylicoin ของสมาชิกคนไหนก็ได้ | **แก้แล้ว** |
| `POST /api/member/deduct-totalpoint/:tel` | เหมือนกัน และ `UpdateTotalPoint` ไม่เรียก `GetUserIdFromClaims` เลย = ไม่มีใครถูกบันทึกว่าเป็นคนทำ | **แก้แล้ว** |
| `POST /api/card/clear-card` | ล้างมูลค่าทั้งใบ ขณะที่ `DELETE card/:cardNo` ซึ่งเบากว่ากลับถูก guard | **ไม่ติด — เจ้าของระบบตัดสินแล้ว** |
| `POST /api/claim-prize`, `POST /api/claim` | หักยอดบัตรผ่าน `CreateCardWithdraw`/`UpdateCardDepositBalance` แต่ไม่ถูก guard ทั้งที่ `refund-confirm` ข้าง ๆ ถูก guard | **ไม่ติด — เจ้าของระบบตัดสินแล้ว** |

สามตัวแรกติด `RequireRole(1-4)` แล้วใน `7647056` — ตรวจก่อนติดว่า **ไม่มี BFF ตัวไหนเรียกเลย**
(`card/tel/[tel]` ยิง `GET /member/:tel` ซึ่งคนละ method) จึงไม่กระทบเส้นทางหน้าจอ
ทดสอบแล้ว roleId 7 → 403, roleId 3 → 200 ทั้งสามตัว

**สามตัวหลัง: เจ้าของระบบตัดสินแล้วเมื่อ 18 ก.ย. ว่าไม่ต้องให้หัวหน้าอนุมัติ**
การล้างบัตรและการแลกของรางวัลเป็นงานที่พนักงานหน้าร้านทำเป็นปกติ
**ถือเป็นข้อยุติ ไม่ใช่งานค้าง** — อย่าไปเพิ่ม `RequireRole` ให้สาม route นี้ภายหลัง
โดยอ้างว่า "เพื่อความสม่ำเสมอกับ refund-confirm"

เหตุผลประกอบที่ทำให้การตัดสินนี้สมเหตุสมผล: การ gate ไว้ที่ role 1-4 เป็นของหลอก
ในวันนี้อยู่แล้ว เพราะ BFF ยิงด้วย service account ที่เป็นแอดมิน
(ดู [[shared-service-account-limits-authz]]) และจะกลายเป็นตัวบล็อกงานปกติ
ทันทีที่ส่งตัวตนพนักงานจริงขึ้นไป

สิ่งที่คุ้มครองสาม route นี้จึงเป็น**ความถูกต้องของตรรกะ** ไม่ใช่ด่านสิทธิ์ —
ซึ่งแก้ไปแล้วในรอบนี้ (claim ตรวจยอดก่อนสร้างแถว, guard กัน ClearCard
ล้างบัตรที่ยังมียอด, หักยอดทีละคู่เรียงกัน)

### ~~3. `card_deposit_service.go:240` ปิด card play โดยดูแค่ coin~~ — แก้แล้ว 24 ก.ย. `aa99141`

`deposit[0].BalanceCoin == 0` ไม่ดู `BalanceBonus` — โปรโมชันที่ให้ bonus อย่างเดียว
(`ECoin = 0`, `EBonus = 300`) จะถูกปิดสิทธิ์เล่นทั้งที่ยอด bonus ยังอยู่ครบ

บล็อกนี้ไม่เคยทำงานบนเส้นทางสำเร็จมาก่อน (เงื่อนไข `if err != nil` กลับด้าน แก้ใน `fbcd244`)
บั๊กจึงหลับอยู่ ตอนนี้ทำงานทุกครั้งที่หักยอด

**เกิดขึ้นจริงแล้วหนึ่งครั้ง** ตรวจ UAT 24 ก.ย.: บัตร `F443416C` ถูกปิดสิทธิ์เล่นเมื่อ
`2026-09-18 11:08:07` ขณะเหลือ bonus 140 — `card_play.delete_date` ห่างจาก
`card_deposit.update_date` เพียง 0.13 วินาที จึงเป็นเส้นทางนี้แน่นอน
(แถว `card_play` ที่ปิดไปแล้วทั้งที่ deposit ยังมียอดมี 60 แถว แต่ที่เหลือปิดก่อน
`fbcd244` ทั้งหมด จึงมาจากเส้นทางอื่น)

**แก้แล้ว** ปิดต่อเมื่อหมดครบทั้งสามยอด — `coin == 0 && bonus == 0 && discount_cash <= 0`
เทียบ discount cash ด้วย `<= 0` เป็นการกันไว้ก่อน (เหตุผลเรื่องเศษทศนิยมที่เคย
เขียนไว้ตรงนี้ผิด — คอลัมน์เป็น `numeric(8,2)` ดูข้อ 10 ใน SECURITY_AUDIT ฉบับแก้แล้ว)

เลือกเกณฑ์ที่ปิดยากกว่าโดยตั้งใจ: ปิดช้าไปหนึ่งครั้งไม่เสียหาย เพราะครั้งถัดไปยอดไม่พอ
`UPDATE` ก็ไม่โดนแถวอยู่แล้ว แต่ปิดเร็วไปคือการยึดสิทธิ์ที่ลูกค้าจ่ายเงินมาแล้ว

**ปลดสถานะของ `F443416C` คืนแล้ว 24 ก.ย.** ตามที่เจ้าของระบบสั่ง

บัตรใบนี้มี `card_play` ที่ถูกปิดอยู่สองแถว แต่**ปลดคืนแค่แถวเดียว**:

| `card_play` | `card_deposit` | ปิดเมื่อ | ยอดที่เหลือ | ทำอะไร |
|---|---|---|---|---|
| `ef64d6be…` | `145daef8…` | 18 ก.ย. 11:08:07 | coin 0 / **bonus 140** | **ปลดคืน** |
| `0373762d…` | `58b54e38…` | 18 ก.ย. 11:03:19 | coin 0 / bonus 0 | ไม่แตะ — ปิดถูกแล้ว |

`UPDATE card_play SET is_delete = false, delete_date = NULL` เจาะจงด้วย `id`
และบังคับ `AND is_delete = true` รันใน transaction ที่มี `GET DIAGNOSTICS`
คอยยกเลิกทั้งชุดถ้าจำนวนแถวที่โดนไม่เท่ากับ 1 พอดี

ยืนยันหลัง commit ด้วย connection ใหม่: เหลือแถวที่ปิดอยู่แถวเดียวคือ `0373762d…`
และ**ยอดในบัตรไม่ขยับเลย** (coin 7,630 / bonus 970 / discount 350.00 และ
`max(update_date)` ยังเป็น `2026-09-18 11:08:08.558835` เท่าเดิม) แปลว่าไม่ได้
ไปแตะแถวเงินสักแถว · snapshot ก่อนแก้เก็บไว้ที่ scratchpad `F443416C_before.txt`

### ~~4. `GenerateBillNo` ใช้คนละเขตเวลากับ `bill_date`~~ — แก้แล้ว 18 ก.ย.

`pos_transaction_service.go:451` ใช้ `time.Now()` (เขตเวลา process) ขณะที่ `bill_date`
เขียนด้วย `utils.TimeNowAsia()` — ตรวจแล้ว container รัน **UTC** จริง (`TZ` ว่าง)

บิลที่ออกระหว่าง 00:00–07:00 ตามเวลาไทยจะมีวันที่ในเลขบิลเป็นของเมื่อวาน
ขณะที่ `bill_date` เป็นวันนี้ และตัวนับรีเซ็ตตอน 07:00 ไม่ใช่เที่ยงคืน
ขัดกับจุดประสงค์ของการแก้ C5

ที่อื่นก็ใช้ `time.Now()` แบบเดียวกันอีก ~10 จุด (`deposit_service.go:93`,
`redeem_service.go:86`, `card_play_service.go:409,508` …)

หมายเหตุพ่วง: `utils.TimeNowAsia()` เรียก `panic(err)` ถ้าโหลด `Asia/Bangkok` ไม่ได้
ตอนนี้ปลอดภัยเพราะ `Dockerfile` ใช้ `FROM golang:1.24` ซึ่งมี tzdata
**ถ้าย้ายไป base image เล็กลงตอน deploy จะ panic ทันที** ต้อง `import _ "time/tzdata"`
หรือติดตั้ง tzdata (เขียนคำเตือนนี้ไว้ในคอมเมนต์ของฟังก์ชันแล้ว)

**แก้แล้ว** เปลี่ยน `GenerateBillNo`, `GenerateDepositBillNo`, `GenerateRedeemBillNo`
ให้ใช้ `utils.TimeNowAsia()` สร้างทั้งหน้าต่างนับและวันที่ในเลข
สองตัวหลังแบ่งช่วงเป็นเดือน ความเพี้ยนจึงมีผลเฉพาะ 7 ชั่วโมงสุดท้ายของวันสิ้นเดือน

พิสูจน์ด้วยการจำลองตรรกะเก่า/ใหม่ที่เวลาต่าง ๆ (ทดสอบตอนกลางวันไม่เห็นความต่าง
เพราะบั๊กแสดงตัวเฉพาะ 00:00–07:00 เวลาไทย = 17:00–24:00 UTC ของวันก่อนหน้า):

| UTC | เวลาไทย = `bill_date` | เลขบิลเดิม | เลขบิลใหม่ |
|---|---|---|---|
| 09-18 10:02 | 09-18 17:02 | `...260918` | `...260918` ตรงกัน |
| 09-18 17:00 | **09-19** 00:00 | `...260918` ผิดวัน | `...260919` |
| 09-18 18:30 | **09-19** 01:30 | `...260918` ผิดวัน | `...260919` |
| 09-18 23:59 | **09-19** 06:59 | `...260918` ผิดวัน | `...260919` |

ยิงจริงหลังแก้: `UJN-01-2609180018` กับ `bill_date 2026-09-18 17:03` → `260918` ตรงกัน

พ่วง: เอา `fmt.Println` 3 บรรทัดออกจาก `TimeNowAsia()` — มันถูกเรียกในทุกเส้นทาง
ที่ขยับเงิน เสียงรบกวนกลบ log ที่ใช้ตามปัญหาจริง ยืนยันแล้วว่าเหลือ 0 บรรทัด

**~~ข้อสังเกตที่ยังไม่แก้:~~ แก้แล้ว 24 ก.ย. `a41bd06`** เลขบิลเคยมาจาก `COUNT(*)`
ของวันนั้น ถ้ามีการ **ลบ** บิล เลขจะถูกใช้ซ้ำ (เจอตอนทดสอบ — ลบบิลทดสอบแล้ว
บิลถัดไปได้เลขเดิม) และ `COUNT` ยังนับบิลของ pos นั้นทุกใบไม่ว่ามาจาก stack ไหน
ตัวนับของแต่ละ stack จึงกระโดดตามปริมาณงานของอีกฝั่ง

**แก้โดยอ่านเลขสูงสุดที่เคยออกแล้วแทนการนับแถว** — `MAX(substring(bill_no from '(\d{4})$'))`
ของเลขบิลที่ขึ้นต้นด้วย `prefix-posid-YYMMDD` ของตัวเอง แก้ทั้งสองข้อพร้อมกัน
และตัดการพึ่ง `bill_date` ออกไปเลย (ตัวนับได้มาจากเลขบิลเอง ไม่ใช่จากวันที่ในอีกคอลัมน์)

ทดสอบกับข้อมูลจริง 3 เคส: วันที่มี 1 ใบ → ได้ 2 · วันที่มีเลขซ้ำ `0003` → ได้ 4
(`COUNT` จะให้ 5 แล้วข้าม `0004`) · วัน/prefix ใหม่ → ได้ 1

**เลขบิลซ้ำที่มีอยู่จริงในฐาน — ตรวจ 24 ก.ย.** มี 23 กลุ่ม 104 แถว แยกได้ว่า
6 กลุ่มเป็นแถวที่ถูกก๊อป (`bill_date` เท่ากันถึงไมโครวินาที เช่น `JYN-9-2605240001`
ซ้ำ 21 แถวที่เวลาเดียวกันเป๊ะ) และ 17 กลุ่มเป็นการชนจริง ซึ่งเกือบทั้งหมดเป็น
`KIOSK...` กับค่าขยะอย่าง `string` และ `test`

**ที่มาจากเส้นทางนี้จริงมีใบเดียว** — `JYN-74-2604060003` สองใบ เวลา 00:09 กับ 00:11
ของวันที่ 7 เม.ย. แต่เลขบิลเขียนวันที่ 6 ซึ่งเป็นอาการของบั๊กเขตเวลาที่แก้ไปแล้ว
เมื่อ 18 ก.ย. (`11bb91c`) ไม่ใช่อาการของ `COUNT`

**ที่ยังไม่ปิด:** สองคำขอพร้อมกันอ่านเลขเดียวกันแล้วได้เลขซ้ำ ปิดจริงต้องมี
unique index บน `bill_no` ซึ่งเป็น schema ของฐานที่ใช้ร่วมกับระบบเก่า และข้อมูล
ตอนนี้มีเลขซ้ำอยู่แล้ว 23 กลุ่ม จึงสร้าง index ไม่ผ่านจนกว่าจะล้างข้อมูลก่อน
เป็นการตัดสินใจแยก

### ~~5. หน้า adjust แถว E-Stamp ยังไม่ถูก~~ — แก้แล้ว 18 ก.ย.

`pos-fe-new/src/app/(adjust)/adjust/page.jsx` — วันนี้แก้จาก `member.ecoin`
เป็น `member.estamp` ซึ่ง**ถูกแค่ครึ่งเดียว**

`adjust_point_controller.go:69-75` ตรวจเพดานการหัก `adj_estamp` กับ `findMember.Ecoin`
ส่วน `member_service.go` เอา `AdjEstamp` ไปบวกทั้ง `ecoin` และ `estamp`
สมาชิกที่ `estamp = 500` แต่ `ecoin = 100` จะเห็นหน้าจอ 500 ใส่ −500 แล้วได้
`400 Estamp ไม่เพียงพอ ในระบบมี 100` ซึ่งเป็นตัวเลขที่ไม่ปรากฏบนหน้าจอเลย

**แก้แล้ว — แสดงทั้งสองค่า**

ตรวจข้อมูลจริง 2026-09-18 ก่อนตัดสินใจ: สมาชิก **8198 คน มีแค่ 16 คน**
ที่ `ecoin = estamp` ~~ส่วนใหญ่ `estamp = 0` ขณะที่ `ecoin` มีค่าจริง
(สูงสุดที่เจอ `ecoin = 360,365` ส่วน `estamp = 0`)~~

> **ตัวเลขที่ขีดฆ่าผิด** (ตรวจซ้ำ 24 ก.ย.) — `estamp = 0` มีแค่ 23 คนจาก 8198
> ไม่ใช่ "ส่วนใหญ่" และ `max(estamp) = 1000` ข้อสรุปทั้งย่อหน้าถัดไปที่สร้างบนตัวเลขนี้
> จึงใช้ไม่ได้ ดูหัวข้อ ⚠️ ท้ายข้อ 5

ฉะนั้นการโชว์ค่าเดียวจะหลอกไม่ทางใดก็ทางหนึ่ง — โชว์ `estamp` อย่างเดียว
จะขึ้น 0 ให้เกือบทุกคนทั้งที่หักได้จริง ส่วนโชว์ `ecoin` อย่างเดียวก็เป็นเลข
ที่ไม่ตรงกับป้าย (การแก้ครั้งแรกเมื่อ 17 ก.ย. เปลี่ยนจาก `ecoin` เป็น `estamp`
จึงทำให้แย่กว่าเดิมสำหรับกรณีทั่วไป)

ตอนนี้แถวนี้แสดง `estamp` เป็นค่าหลัก พร้อมบรรทัดเล็กบอก `eCoin <ยอด>`
และกำกับว่า "หักได้ไม่เกิน eCoin"

ข้อความ error ฝั่ง backend ก็แก้ให้ตรงด้วย — เดิม `"Estamp ไม่เพียงพอ ในระบบมี <ecoin>"`
อ่านแล้วงงเพราะเลขที่อ้างเป็นของ eCoin ตอนนี้เป็น
`"หัก E-Stamp ได้ไม่เกินยอด eCoin ซึ่งมีอยู่ 895 (ขอหัก 99999)"` (ทดสอบแล้ว)

## ⚠️ แก้ความเข้าใจผิดเรื่อง `ecoin` / `estamp` (24 ก.ย.)

**เจ้าของระบบยืนยัน 2026-09-24: ในตาราง `member` ยอด E-Stamp อยู่ที่คอลัมน์ `ecoin`
ส่วนคอลัมน์ `estamp` ไม่ได้ใช้งาน** ทุกข้อสรุปด้านบนที่ตั้งอยู่บนสมมติฐานว่า
สองคอลัมน์นี้คือยอดเดียวกันจึงผิด และของที่ตามมามีสามอย่าง:

1. **หน้าจอโชว์ผิดช่องมาตลอด** — การแก้เมื่อ 17 ก.ย. เปลี่ยนจาก `ecoin` เป็น `estamp`
   และการแก้เมื่อ 18 ก.ย. ที่โชว์ `estamp` เป็นค่าหลัก ทั้งคู่โชว์คอลัมน์ที่ไม่ได้ใช้
   ตอนนี้โชว์ `ecoin` ค่าเดียว
2. **หน้า adjust เขียนทับข้อมูลที่ sync มาจากระบบ E-Stamp** — `UpdateAdjustPoint`
   บวก `adj_estamp` ลง `estamp` ด้วย ทั้งที่คอลัมน์นั้นเป็นกระจกของระบบภายนอก
   ตัดการเขียนทิ้งแล้ว
3. **การปัดพื้น `estamp` ที่ทำไปตอนเช้าเป็นการแก้ผิดจุด** — ถอนออกแล้ว
   ไม่ต้องกันติดลบในคอลัมน์ที่ไม่ได้เขียนอีกต่อไป

**ตัวเลขในรายงานฉบับ 18 ก.ย. ที่ว่า "ส่วนใหญ่ `estamp = 0`" ผิด** — ตรวจซ้ำ 24 ก.ย.
สมาชิก 8198 คน มี `estamp = 0` แค่ **23 คน** และ `max(estamp) = 1000`
ขณะที่ `max(ecoin) = 360,365` สองค่านี้เป็นคนละปริมาณกัน ไม่ใช่ค่าเดียวกันที่บางแถวหาย
(ที่ถูกคือ `ecoin = estamp` มีแค่ 16 คน ตัวเลขนี้ตัวเดียวที่ยังใช้ได้)

ข้อความ error ฝั่ง backend ตัดส่วนที่อธิบายว่า "หักได้ไม่เกินยอด eCoin" ออก
เหลือ `"E-Stamp ไม่เพียงพอ ในระบบมี <ecoin> (ขอหัก <n>)"` เพราะ eCoin กับ E-Stamp
เป็นยอดเดียวกัน ไม่ใช่สองยอดที่ต้องเอามาเทียบกัน

ทดสอบ: `UPDATE` ตัวใหม่ (12 parameter) ยิงเข้า UAT ด้วยเบอร์ที่ไม่มีอยู่จริง
ได้ `UPDATE 0` ไม่แตะแถวใคร · `go build` + `go vet` ผ่าน · `yarn build` ผ่าน

### ไล่ความเสียหายของ `estamp` ที่ถูกหน้า adjust เขียนทับ — ตรวจแล้ว 24 ก.ย. ไม่ต้องกู้

`adjust_point` เป็นสมุดบันทึกทุกรายการปรับ มี 39 แถว ในนั้นแตะ E-Stamp **12 แถว
จาก 3 เบอร์ และเป็นการบวกเพิ่มทั้งหมด ไม่มีการหัก** ครั้งล่าสุด 2026-06-09

| เบอร์ | ครั้ง | รวมที่บวกลง `estamp` | ล่าสุด | POS `estamp` วันนี้ | ต้นทาง E-Stamp |
|---|---|---|---|---|---|
| 0958436474 | 7 | +972 | 2026-06-09 | 0 | 0 |
| 0914182425 | 1 | +200 | 2026-06-05 | 200 | 200 |
| 0903325999 | 4 | +60 | 2025-10-21 | 0 | 0 |

**ไม่มีใครเพี้ยนจากต้นทาง** เพราะหน้า adjust ค้นสมาชิกผ่าน
`/api/member/sync-card/` → `/member/search-sync/:tel` → `UpdatePosMemberScore`
ซึ่ง**เขียนทับ `estamp` `ecoin` `bonus` `total_point` ทั้งชุดจากต้นทางทุกครั้งที่ค้นสมาชิก**
ค่าที่หน้า adjust เขียนลง `estamp` จึงอยู่ได้แค่จนกว่าจะมีใครค้นเบอร์นั้นอีกครั้ง

**ข้อที่พิสูจน์ไม่ได้:** เบอร์ `0914182425` มี 200 ทั้งสองฝั่ง ซึ่งเท่ากับยอดที่ปรับพอดี
แยกไม่ออกว่าต้นทางมี 200 อยู่ก่อนแล้ว POS ถูก sync ทับกลับ หรือ +200 ถูกดันออกไป
ถึงต้นทางผ่าน `SyncScoreMember` ที่หน้า adjust ยิงเข้า CRM ข้อที่ชวนสงสัยคือ
ต้นทางมีสมาชิก 1,214,433 คนแต่มี `estamp <> 0` แค่ **27 คน** และเบอร์นี้เป็นหนึ่งใน 27
— ไม่ใช่หลักฐาน ถ้าอยากรู้แน่ต้องดู log ฝั่ง CRM ของวันที่ 2026-06-05

**เจ้าของระบบให้ความเห็น 24 ก.ย. ว่าน่าจะเกิดจากการทดสอบข้อมูล** ซึ่งเข้ากับรูปของข้อมูล:
ทั้งสามเบอร์เป็นการบวกอย่างเดียว ยอดกลม (60 / 200 / 972) และกระจายห่างกันเป็นเดือน
ไม่ใช่รูปของงานหน้าร้านจริง **ปิดข้อนี้ ไม่ต้องกู้ข้อมูล**

**ของแถมที่เจอระหว่างไล่:**
- ต้นทางก็ใช้ `ecoin` เป็นช่องจริงเหมือนกัน (`ecoin <> 0` 520,882 คน เทียบกับ
  `estamp <> 0` 27 คน) ยืนยันเรื่อง `ecoin` = E-Stamp จากอีกฝั่งหนึ่ง
- ต้นทางมีขยะค้างใน `estamp` เช่น `-2,117,645` และ `-510,210` ถ้าวันหนึ่งมีใคร
  เอาช่องนี้มาใช้จริงจะเจอของแบบนี้รออยู่

### ใครเขียน `estamp` ฝั่ง POS — ตอบแล้ว 24 ก.ย.: ไม่มีใครเขียน เป็นเศษจากการนำเข้าข้อมูล

ตั้งคำถามไว้ว่าทำไมฝั่ง POS มี `estamp <> 0` เกือบทั้งตาราง ทั้งที่ต้นทางแทบไม่มีใครมี
คำตอบอยู่ในรูปของข้อมูลเอง ไม่ต้องเดาว่าโค้ดตัวไหนเขียน:

| หลักฐาน | ค่า |
|---|---|
| ค่าที่ต่างกันทั้งหมดของ `estamp` ในสมาชิก 8,603 คน | **4 ค่า** — 0, 10, 200, 1000 |
| แถวที่ `estamp = 1000` **และ** `finwow = 1000` พร้อมกัน | **8,167** |
| แถวที่ `estamp` เป็นค่าอื่นนอกจาก 0 กับ 1000 | **2** (คือ 200 กับ 10 ที่หน้า adjust เขียน) |
| `column_default` ของคอลัมน์ `estamp` | **0** (ไม่ใช่ 1000) |
| สมาชิกที่ถูกสร้างวันเดียวกันเมื่อ 2025-07-22 | **8,174 คน** — ในนั้น 8,167 คนได้ 1000/1000 |

**สรุป: เลข 1000 มาจากการนำเข้าข้อมูลครั้งเดียวเมื่อ 2025-07-22** ที่ยัดค่าคงที่ลงทั้ง
`estamp` และ `finwow` ของทุกคนในชุดนั้น ไม่ใช่ค่าที่ระบบไหนเขียนอยู่ทุกวัน สมาชิก
ที่ถูกสร้างหลังจากนั้นทุกคนได้ `estamp = 0` ตาม default ยกเว้นสองคนที่หน้า adjust แตะ

**ข้อสรุป "`estamp` ไม่ได้ใช้งาน" จึงพิสูจน์ได้จากพฤติกรรมแล้ว ไม่ใช่แค่เจตนา** ปิดข้อนี้

**⚠️ แต่เจอของที่ใหญ่กว่าระหว่างทาง — `finwow` ก็ปลอมด้วยและตัวนี้มีคนใช้**

`finwow` เป็นค่าคงที่ 1000 ในแถวเดียวกันทั้ง 8,167 แถว มีค่าที่ต่างกันแค่ 4 ค่า
และมีแค่ **2 แถว**ที่เป็นค่าจริงนอกจาก 0 กับ 1000 — รูปเดียวกับ `estamp` เป๊ะ

ต่างกันตรงที่ **`finwow` ไม่ใช่ช่องที่ถูกทิ้ง** หน้า `/adjust` มีแถว FinWow ให้พนักงาน
ปรับได้ และ `adjust_point_controller.go` ตรวจเพดานการหักด้วย `findMember.Finwow`
บน UAT ทุกคนจึงดูเหมือนมี FinWow 1000 ให้หักได้ ทั้งที่เป็นเลขที่ import มา

**ยังไม่ได้ตรวจว่าโปรดักชันเป็นแบบเดียวกันไหม** ถ้าเป็น แปลว่ายอด FinWow ที่หน้าจอ
แสดงและให้หักได้เป็นของปลอมทั้งระบบ ควรรันคิวรีชุดเดียวกันกับฐานโปรดักชันก่อนสรุป

**⚠️ ตารางสมาชิกบน UAT ขยับเองระหว่างที่เราทดสอบ**

วัดเมื่อ ~11:00 ได้ 8,198 แถว วัดอีกครั้งบ่ายวันเดียวกันได้ **8,603** แถว
ส่วนต่าง 405 แถวเป็นสมาชิกชื่อ `Guest` เบอร์รูปแบบ `2024101003` (ไม่ใช่เบอร์โทร)
ทุกแถวมี `create_date` เดียวกันเป๊ะคือ `2026-09-21 09:15:04.445726` ซึ่งเป็นวันย้อนหลัง
แปลว่ามีงานคัดลอกข้อมูลยัดเข้ามาพร้อม timestamp เดิมของต้นทาง ไม่ใช่การสมัครสมาชิกจริง

สำคัญกับการทดสอบ: **snapshot ที่เก็บไว้ตอนเช้าอาจใช้เทียบตอนบ่ายไม่ได้**
ยังไม่รู้ว่าใครรันงานนี้และรันบ่อยแค่ไหน

การตรวจนี้ query ฐาน E-Stamp (`202.151.188.61`) แบบอ่านอย่างเดียว `SELECT` ล้วน
เครื่องนั้นไม่ใช่ UAT เหมือนตัวอื่นที่เราทำงานด้วย

### ~~6. `claim_prize` commit ก่อนการหักยอด~~ — แก้แล้ว 18 ก.ย.

`CreateClaimPrize` กับตัวนับ JubuJibi commit ก่อนการหักยอดและไม่มีการชดเชย
ถ้าหักยอดล้มเหลวจะได้แถว claim ที่ไม่ได้หัก coin และกดซ้ำได้แถวใหม่เรื่อย ๆ

**ทำซ้ำได้จริง 18 ก.ย.** ขอแลก 99999 จากบัตร `TEST023` ที่มี 500
→ ตอบ error ถูกต้อง แต่ `claim_prize` เพิ่มจาก 38 เป็น 39 โดยไม่หัก coin เลย

**แก้แล้ว** เรียงลำดับใหม่: ตรวจยอดให้ผ่านก่อน (ใช้ `applyCardWithdrawClaim`
แบบอ่านอย่างเดียว) → สร้างแถว claim → หักยอด → พลิกตัวนับ
ตัวนับอยู่คนละฐาน (`DB_JREADER`) จึงรวมเป็นทรานแซกชันเดียวไม่ได้
ทำได้แค่เรียงให้ของที่ย้อนยากที่สุดอยู่ท้ายสุด

ทดสอบหลังแก้: ยอดไม่พอ → **400** ไม่มีแถวใดถูกเขียน · เคสปกติ → 200 ยอด −5 ตรงจำนวน

### 7. ความเร็ว

| จุด | ผล |
|---|---|
| `card_entity_controller.go:713` | ~~เติมเงินที่มี free point เรียก CRM 5 ครั้งเรียงกัน~~ **แก้แล้ว 18 ก.ย.** — cache `GetScoreType`/`GetBranchByCode` (TTL 30 นาที) เหลือ 3 call และแยก `SyncHistory` ออกมาขนาน |
| `main.go:108` | ~~`MaxIdleConns = 2` ขณะที่ `MaxOpen = 10`~~ **แก้แล้ว 18 ก.ย.** — ตั้ง maxIdle = maxOpen ทั้งสี่ฐาน |

**⚠️ ตัวเลขเวลาที่วัดในเครื่องพัฒนาไม่ได้สะท้อน production**

วัดเมื่อ 18 ก.ย.: เติมเงินหนึ่งรายการใช้ 4.65 วินาที แต่พอแยกส่วนแล้วพบว่า
`free_point = 0` (ไม่แตะ CRM เลย) ยังใช้ **1.8 วินาที**

สาเหตุ: ฐานข้อมูลเป็น DigitalOcean managed อยู่บนคลาวด์ ส่วน container รันในเครื่อง
วัดเวลาไป-กลับต่อ query ได้ **73–195 ms** การเติมเงินยิงราว 15-20 query
จึงเป็นเวลาเดินทางบนเครือข่ายล้วน ๆ

บน production ที่ API กับฐานข้อมูลอยู่ศูนย์ข้อมูลเดียวกัน latency จะเหลือ ~1 ms
เวลา 1.8 วินาทีนั้นจะเหลือราว 20 ms **อย่าไปไล่แก้จำนวน query ตามตัวเลขนี้**

ส่วนที่สะท้อน production จริงคือ **CRM** เพราะเป็นระบบภายนอกทั้งสองที่:
วัดได้ ~0.7–1.0 วินาทีหลังทำ cache แล้ว (เดิม ~3 วินาที)
| `card_deposit_service.go:237` | ทุกการหักยอดตามด้วย SELECT อีกหนึ่งครั้ง = 2–3 round-trip แทน 1 และยิงทุกครั้งที่แตะบัตรที่เครื่องเกม ใช้ `RETURNING` ได้ |
| `clearcard/page.jsx` | เคลียร์ทีละใบแบบ `await` ในลูป 10 ใบ ≈ 3 วินาที ทำขนานแบบจำกัดจำนวนได้ |

### ~~8. `Tables.jsx:81` — `editingQty` ค้างข้ามรายการขาย~~ — แก้แล้ว `4c65760` + `3fc324d`

React ไม่ยิง `onBlur` ตอน unmount และ `<Tables>` ถูกเรนเดอร์แบบไม่มีเงื่อนไข
(`topup/page.jsx:1443`) จึงไม่เคย unmount พิมพ์จำนวนแล้วลบแถวทิ้ง คีย์จะค้าง
แล้วไปทับจำนวนที่แสดงในรายการขายถัดไป — หน้าจอโชว์ 12 แต่คิดเงินจาก 1

**แก้แล้ว** `useEffect` ตัด `editingQty` ให้เหลือเฉพาะคีย์ของแถวที่อยู่ในตารางจริง
ครอบทั้งการลบแถวและการล้างตะกร้า โดยผู้เรียกไม่ต้องส่งสัญญาณอะไรมา
ทดสอบตรรกะ 5 เคสผ่าน

⚠️ คอมมิตแรก (`4c65760`) ทำให้ build พัง เพราะแก้ไฟล์ด้วย python heredoc
แล้ว `\n` กลายเป็นขึ้นบรรทัดจริง และการตรวจใช้ `yarn build | tail -3`
ซึ่งสถานะของ pipeline คือสถานะของ `tail` = 0 เสมอ จึงไม่จับความล้มเหลว
`3fc324d` ซ่อมและยืนยันด้วย exit code ตรง ๆ

### ~~9. `Jwt.go:84` — `roleId` เป็น `null` ได้~~ — แก้แล้ว 18 ก.ย. `280dfe3`

สาขา Bearer ใส่ `UserRoleId` ที่เป็น `*int` ลง claim ตรง ๆ ต่างจากสาขา Basic
ที่แปลง nil เป็น 0 อยู่แล้ว **ไม่ใช่ช่องโหว่** — `GetRoleIdFromClaims` ตกเข้า
`default` คืน error แล้ว `RequireRole` ตีความว่าไม่มีสิทธิ์ ปิดตายถูกต้อง
แต่ผู้ใช้เจอ "ตรวจสอบสิทธิ์ไม่ได้" ทุกหน้าโดยไม่มีอะไรชี้ว่าสาเหตุคือ role ว่าง

**ข้อกังวลก่อน deploy ปิดได้แล้ว** — `POS_SERVICE_USERNAME` = `pos_frontend`
= `users.id` 751, `user_role_id` = 1 Administrator, `user_group_id` = 1
และไม่มีผู้ใช้คนไหนใน 927 คนที่ `user_role_id` เป็น NULL

พบเพิ่มระหว่างแก้ข้อนี้ และแก้ไปพร้อมกัน:

| พบ | ผล | commit |
|---|---|---|
| `Authorization: Basic Og==` เข้า API ได้ **200** | ดูหัวข้อถัดไป | `4534817` |
| `AuthenticateWithBranch` deref nil ได้ 5 ทาง | 500 ตอนล็อกอิน (ข้อมูลตอนนี้ยังไม่โดน) | `4534817` |
| `TOKEN_EXPIRE` พัง = `log.Fatalf` ตอน login คนแรก | **ตายทั้งโปรเซส** ย้ายไปตรวจตอนบูต | `280dfe3` |
| `CreateUser`/`UpdateUser` ตั้ง role/group เป็น NULL ได้ | สร้างบัญชีที่ล็อกอินไม่ได้ | `280dfe3` |
| `UpdateUser` id ไม่ใช่ตัวเลข → ตอบ 200 ตัวเปล่า | ดูเหมือนแก้สำเร็จทั้งที่ไม่ได้แตะ DB | `280dfe3` |

เพิ่ม `utils/Jwt_test.go` เป็นไฟล์ทดสอบไฟล์แรกของโปรเจกต์ (4 ตัว 7 เคส
ไม่แตะฐานข้อมูล) เพราะความต่างระหว่างถูกกับผิดคือ `0` กับ `null` ซึ่ง
ล็อกอินผ่านเหมือนกันทั้งคู่ พิสูจน์แล้วว่าย้อนโค้ดกลับแล้วทดสอบ FAIL จริง

### 🔴 เข้า API ได้โดยไม่ต้องมีรหัสผ่าน — แก้แล้ว `4534817` แต่ข้อมูลยังค้าง

ยิงจริงแล้วเข้าได้ ไม่ใช่การกันไว้ล่วงหน้า:

```
curl -H "Authorization: Basic Og==" .../api/user-role   ->  200
```

`Og==` คือ base64 ของ `":"` ถอดได้ชื่อผู้ใช้ว่างกับรหัสผ่านว่าง
ต้องมีสองอย่างพร้อมกันถึงเปิดช่องนี้:

1. **ข้อมูล** — `users` มีแถว **id 24** ที่ `username` และ `password`
   เป็นค่าว่างทั้งคู่ และยัง `is_active = true`, `is_delete = false`
   `FindUserDbByUsername` กรองแค่ `is_active` จึงหาเจอ
2. **โค้ด** — เทียบรหัสผ่านด้วย `user.Password != password` (`"" != ""`
   เป็นเท็จ) และเส้นทาง Basic auth ไม่มีด่านกันค่าว่าง

`controllers.Login` กันค่าว่างอยู่แล้ว แต่ `middlewares/auth.go` เรียก
`services.Authenticate` ตรง ๆ ไม่ผ่านด่านนั้น และ Basic auth ใช้ได้กับ
**ทุก endpoint** จึงเปิดทั้ง API ด้วย header บรรทัดเดียว ได้สิทธิ์ role 7

ฝั่งโค้ดปิดแล้วที่ชั้น service (`rejectEmptyCredentials`) เพราะเป็นจุดเดียว
ที่ผู้เรียกทุกทางต้องผ่าน ยืนยัน: `Basic Og==` 200→401, บัญชีบริการจริงยัง 200

**ครึ่งที่เป็นข้อมูล — ปิดแล้วบน UAT เมื่อ 24 ก.ย.** ตามที่เจ้าของระบบสั่ง

แถว `users.id = 24` เดิมมี `username = ''` และ `password = ''` พร้อม `is_active = true`
ตั้งค่าเป็น `username = 'user24'` รหัสผ่านตามที่เจ้าของระบบกำหนด (เก็บเป็น plaintext
ตามที่ระบบทำอยู่ทุกวันนี้ — ดู B1 ที่ยังเลื่อน bcrypt ไว้)

`UPDATE` เจาะจงด้วย `id = 24` และบังคับว่าทั้งสองช่องต้องว่างจริง รันใน transaction
ที่ยกเลิกทั้งชุดถ้าจำนวนแถวที่โดนไม่เท่ากับ 1 พอดี

ตรวจก่อนเขียน: `user24` ยังไม่มีใครใช้ · มีอีก **20 แถวที่รหัสผ่านว่าง** แต่ทุกแถว
เป็น `is_active = false` จึงไม่ผ่าน `FindUserDbByUsername` อยู่แล้ว
ตรวจหลังเขียน: แถวที่ `username = '' AND password = '' AND is_active AND NOT is_delete`
เหลือ **0** และแถวที่ยัง login ได้ทั้งที่รหัสผ่านว่างเหลือ **0** เช่นกัน

**⚠️ ที่ยังค้าง:**
- **ปิดเฉพาะ UAT** — โปรดักชันเป็นคนละฐาน ยังไม่ได้ตรวจว่ามีแถวลักษณะเดียวกันไหม
  ต้องรันคิวรีชุดเดียวกันกับฐานโปรดักชันก่อนถึงจะบอกได้ว่าปิดครบ
- **ยังไม่ได้ตรวจว่าระบบเดิมมีช่องนี้ด้วยหรือไม่** — ตอนนี้ปิดที่ข้อมูลแล้วทั้งสองระบบ
  จึง login ด้วยรหัสว่างไม่ได้เหมือนกันบน UAT แต่ถ้าระบบเดิมเทียบรหัสผ่านแบบเดียวกัน
  มันก็ยังรับ `Basic Og==` ได้ทันทีที่มีแถวว่างโผล่มาใหม่ — ตัวโค้ดฝั่งนั้นยังไม่ถูกแก้

### ~~10. PII ใน log~~ — แก้แล้ว 18 ก.ย.

`[ORPHAN DEPOSIT]` (`card_entity_controller.go:663`) เขียนเลขบัตร ชื่อแคชเชียร์ สาขา
และ `[VOID]` (`pos_void_controller.go:299`) เขียนเบอร์โทรลูกค้า ลง stdout
ซึ่งไหลเข้า log stream ที่มีคนอ่านได้กว้างกว่าฐานข้อมูลและไม่มี retention policy

**แก้แล้ว** เพิ่ม `utils.MaskTel()` แล้วใช้กับทุกจุดที่ log เบอร์โทร
เก็บสามตัวหน้ากับสามตัวท้าย (`0925359415` → `092xxxx415`) พอให้เทียบกับ
รายการในฐานข้อมูลได้เวลาไล่ปัญหา แต่ไม่พอจะเอาไปติดต่อลูกค้า
`0000000000` ไม่ปิดบังเพราะไม่ใช่ข้อมูลส่วนบุคคล — ทดสอบ 7 เคสผ่าน

จุดที่แก้: `[TOPUP]` `[CLAIM]` ×2 `[VOID]` `[CARD]` ×2 และ `discountcash`
และลบ `fmt.Println("Telephone:", tel)` ใน `deposit_controller`
กับ `refund_request_controller` ทิ้ง — พิมพ์เบอร์เต็มทุกคำขอโดยไม่ได้ใช้ทำอะไร

**สิ่งที่ตั้งใจเก็บไว้ ไม่ได้ลบ:**
- `card_no` — เป็นตัวระบุภายในของบัตร ไม่ใช่ข้อมูลส่วนบุคคลในตัวมันเอง
  และเป็นคีย์ที่ต้องใช้ตามหาปัญหา ถ้าลบด้วย log จะหมดประโยชน์
- `cashier` ใน `[ORPHAN DEPOSIT]` — รายงานเดิมบอกว่าซ้ำซ้อนเพราะดูจาก
  `bill_no` ได้ **แต่ไม่จริงในเคสนี้** เพราะ orphan แปลว่า `pos_transaction`
  ไม่มีอยู่ ชื่อแคชเชียร์จึงหาจากฐานข้อมูลไม่ได้เลย ต้องเก็บไว้ใน log

---

## พบใหม่ระหว่างทดสอบ 18 ก.ย. (ไม่ได้อยู่ในรีวิวเดิม)

### แก้แล้ว

| เรื่อง | commit |
|---|---|
| void แตกสาขาตาม**ชนิดบัตร** ไม่ใช่ตาม**เมนูที่ขาย** → บิลเติมเงินธรรมดาบนบัตร Package ยกเลิกไม่ได้เลย **31 ใบ** | `2cc3dec` |
| `ClearCard` ล้างบัตรที่ยังมียอดดิบ (ดูรายละเอียดด้านล่าง) | `2cc3dec` |
| `handleCardPackageVoidProcess` อ่าน `existSub[0]` โดยไม่เช็คความยาว → panic (มีบิลไม่มี sub 3 ใบ) | `2cc3dec` |
| log `used_time` ขึ้นทุกครั้งที่แตะบัตรบน entitlement 414 แถวที่ `play_time = 0` | `acf8a9f` |
| `UpdateMemberTelToCardEntity` เขียนทับเบอร์ผู้ถือบัตรโดยไม่ตรวจว่าเบอร์ปลายทางมีตัวตน — พิมพ์ผิดหนึ่งตัว บัตรเปลี่ยนเจ้าของเงียบ ๆ ยอดเดิมหายจากสายตา (ประวัติ 86 ครั้งที่โอนระหว่างสมาชิก มี 11 ครั้งที่เบอร์ปลายทางไม่มีตัวตน) | `d78fa8f` |
| ผู้จัดการอนุมัติทับกรณียอดไม่พอ (backend + หน้าจอ) | `c39087b` `868efc4` `e0e25c9` |

**เหตุการณ์ `ClearCard`** — ยกเลิกบิล 100 coin บนบัตร `F443416C` ที่มียอดดิบ 7630/1130
แล้วระบบล้างบัตรทั้งใบ ปิด deposit 72 แถว ลบ `card_play` 40 แถว รีเซ็ต `member_tel`
และบันทึก `clear_card` ว่ายอด 0/0 (กู้คืนครบแล้ว — ยอดไม่ได้ถูกลบ แค่ถูกปิดสถานะ)

ต้นเหตุ: `handleCardVoidProcess` เช็คแค่ `CheckCardInfo` ซึ่ง**ไม่ใช่ยอดดิบ**
เพิ่ม guard ชั้นสองด้วย `FindCardDepositByCardNo` แล้ว ยืนยันด้วย log จริง:
`[VOID] ... ไม่ล้างบัตร เพราะ CheckCardInfo รายงาน 0/0 แต่ยอดดิบยังเหลือ 7630 coin`

### ยังไม่แก้

**ก. ~~`CheckCardInfo` รายงานยอดเป็น 0~~ — ถอนข้อค้นพบ ไม่ใช่บั๊ก**

เคยรายงานไว้ว่า `LEFT JOIN LATERAL` ที่รวมเฉพาะ deposit ที่ `member_tel` ตรงกับ
`card_entity.member_tel` เป็นบั๊กที่ทำให้ลูกค้ามองไม่เห็นเงินตัวเอง — **ไม่จริง**

ตรวจข้อมูลจริงแล้ว บัตรที่ยอดมองไม่เห็นเป็นบัตรที่**เปลี่ยนเจ้าของ**:
`F443416C` มี deposit ถือ 5 เบอร์ต่างกัน, `DD199042` มี 4 เบอร์
การไม่แสดงยอดของเจ้าของเก่าให้เจ้าของใหม่คือสิ่งที่ถูกต้อง — เงินติดตามสมาชิก
ไม่ใช่ติดตามบัตร ตัวกรองนี้จึงเป็นกฎธุรกิจ ไม่ใช่ข้อบกพร่อง

บัตรใบที่สาม (`E447913B`, 280 coin) ที่เคยนับรวมไว้ เป็นความเสียหายจากการทดสอบ
เอง — การทดสอบเมื่อ 17 ก.ย. ใส่เบอร์สมมติ `0812345678` ลง payload แล้วเส้นทาง
เติมเงินเขียนทับเบอร์ผู้ถือบัตรของลูกค้าจริง (กู้คืนแล้ว 18 ก.ย.)

**สิ่งที่แก้จริงคือต้นเหตุ** — `UpdateMemberTelToCardEntity` ตอนนี้ไม่เปลี่ยน
เจ้าของบัตรถ้าเบอร์ปลายทางไม่ใช่สมาชิกที่มีตัวตน ดู `d78fa8f`

**ข. เล่นเกินโควตาแพ็กเกจได้ — เจ้าของระบบตัดสินแล้วว่ายังไม่บล็อก**

`UpdateCardPlayTypeUsed` ใช้ `used_time < play_time` ถ้าโควตาหมดจะคืน error
แต่ `DeductCardPlay` แค่ log แล้วเดินต่อ ฉะนั้นถ้ายอดในบัตรยังเหลือก็เล่นต่อได้

**ตัดสินเมื่อ 18 ก.ย.: ยังไม่ต้องบล็อก** พฤติกรรมปัจจุบันจึงถูกต้องตามที่ต้องการ
ไม่ใช่งานค้าง — เพดานที่ใช้จริงคือยอดในบัตร ไม่ใช่จำนวนครั้ง

ยังคง log ไว้เพื่อให้เห็นว่าเกิดขึ้นบ่อยแค่ไหน ถ้าวันหนึ่งอยากบล็อก
ข้อมูลจาก log จะบอกได้ว่ากระทบเท่าไร (จุดแก้อยู่ที่ `card_play_service.go`
ตรงที่เรียก `UpdateCardPlayTypeUsed` — เปลี่ยนจาก log เป็น return error)

หมายเหตุ: entitlement ที่ `play_time = 0` มี 414 แถวและไม่มีโควตาจำนวนครั้ง
อยู่แล้ว โค้ดจึงข้ามการนับให้ทั้งหมด เรื่องนี้ไม่เกี่ยวกับการบล็อก

**ค. ~~`void_reason` ในโมเดล Go เขียน `varchar(100)` แต่คอลัมน์จริงเป็น `varchar(500)`~~ — แก้แล้ว 24 ก.ย. `aa99141`**

ไม่กระทบตอน insert (GORM ใช้ type tag เฉพาะตอน migrate) แต่ถ้าวันหนึ่งรัน
AutoMigrate จะหดคอลัมน์และตัดข้อมูลทิ้ง — และเส้นทาง ForceVoid เขียนส่วนต่างยอด
ลงช่องนี้ ข้อความจึงยาวเกิน 100 ได้จริง

**เจอเพิ่มระหว่างแก้:** `pos_void.bill_no` tag เขียน `varchar(50)` แต่คอลัมน์จริงเป็น
`varchar(100)` (คนละขนาดกับ `pos_transaction.bill_no` ที่เป็น 50 จริง ๆ) แก้ไปพร้อมกัน

ทั้งสองค่ายืนยันด้วย `information_schema.columns` ของ UAT ไม่ได้เชื่อรายงานเดิม
— ควรไล่ tag ตัวอื่นเทียบคอลัมน์จริงด้วย เพราะเจอผิด 2 จาก 3 ช่องที่สุ่มตรวจในตารางเดียว

---

## ตรวจแล้วสะอาด

- ลำดับ middleware ของ `RequireRole` ถูกต้อง และ fail closed ทุกทาง (`roleId = 0` ไม่ผ่าน)
- `Jwt.go` ตรึง signing method เป็น `*jwt.SigningMethodHMAC` — `alg=none` และ
  RS256→HMAC confusion ทำไม่ได้ และตรวจ `exp` จริง
- `/api/auth/login` ถูกข้ามด้วยการเทียบ path ตรง ๆ ก่อนเช็ค header
- ไม่มี route ซ้ำหรือ v2 ที่วิ่งไปถึง controller ที่ถูก guard
- `buildQuery` ใช้ `URLSearchParams` ไม่มีช่อง injection และแก้บั๊กคำว่า `"null"` ได้จริง
- `upstream.js` อ่าน body ครั้งเดียว ไม่ได้เพิ่ม latency และ service token ถูก cache ตาม `exp`
- API route ทั้ง 104 ตัวมี `apiGuard` ครบ ยกเว้น `auth/*` `device` `get-mac` `qz/*`
  ซึ่งเป็น endpoint ก่อน login โดยตั้งใจ
- `FindUserDbByUsername` กรอง `is_active AND NOT is_delete` — ผู้ใช้ role Disabled
  626 คน **login ไม่ได้เลยสักคน**
- CRM client ทุกตัวมี `Timeout: 10 * time.Second` (`crm_service.go`)
- `station/mac-address` ไม่ได้ถูกลบ (ลบแค่ helper ที่ตายแล้ว) และ `topupStore.js`
  เป็นไฟล์ว่างที่ไม่มีใคร import

---

## รายการทดสอบ

เส้นทางเงินทั้ง 5 (ข้อ 1-5) ทดสอบครบแล้ว เหลือข้อ 6-7 ที่ต้องทดสอบบนเบราว์เซอร์

| # | รายการ | สถานะ |
|---|---|---|
| 1 | เล่นเกมด้วยบัตรที่มี `card_play_type` | **ผ่าน** 18 ก.ย. ยอด −1 ตรงจำนวน 2 ใบ แถวขยะคงที่ 221 |
| 2 | ยกเลิกบิล | **ผ่าน** หักจาก deposit ที่ถูกต้องใบเดียว ยิงซ้ำ 409 |
| 3 | คืนเงิน | **ผ่าน** ยอด −10 ตรงจำนวน `refund_sts` Y→N ยิงซ้ำ 409 ไม่หักอีก |
| 4 | แลกของรางวัลด้วยบัตร | **ผ่าน** 18 ก.ย. หักยอดก่อนสร้างแถว ยอดไม่พอไม่เกิดแถว claim |
| 5 | เติมเงินให้สมาชิกที่มี free point | **ผ่าน** 18 ก.ย. (วัด CRM ได้ด้วย — ดูข้อ 7) |
| 6 | กล่องยืนยันอนุมัติทับบนเบราว์เซอร์ | ยังไม่ทดสอบ — ใช้บิล `JYN-9-2603200013` ขาด 290 |
| 7 | ด่าน elevation หน้า `/adjust` ด้วย role ต่าง ๆ | ยังไม่ทดสอบ |

ข้อ 1-5 ยืนยันด้วยการเทียบ snapshot ฐานข้อมูลก่อน/หลังทุกครั้ง ไม่ใช่ดูแค่ HTTP 200
ต้องเทียบที่ `update_date` ของแถวที่เจาะจง ไม่ใช่ผลรวมยอด — ผลรวมบังการหักที่ผิดแถวได้

**ข้อควรระวังสำหรับคนที่ทดสอบต่อ** — ระบบนี้มี "ยอดคงเหลือในบัตร" อยู่ **4 นิยาม**
ที่ไม่เหมือนกัน การเลือกผิดตัวทำให้สรุปผลผิดได้ง่ายมาก (เกิดขึ้นหลายรอบระหว่างทดสอบ):

| นิยาม | ใช้ที่ไหน |
|---|---|
| `sum WHERE is_active` | มาตรวัดทั่วไป — **มองไม่เห็น deposit ที่ถูกปิดแต่ยังมียอด** |
| `FindCardDepositByIds` | deposit ของบิลเอง — ไม่กรองอะไรเลย |
| `FindCardDepositByCardNo` | `is_active` และต้องมียอด > 0 |
| `CheckCardInfo` | ต้อง `member_tel` ตรงกันด้วย (ดูข้อ ก.) |

ให้ตรวจด้วย `update_date` ว่าแถวไหนถูกแตะจริง แทนการดูยอดรวม
